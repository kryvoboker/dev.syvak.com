name: "dev-syvak"
services:
  dev-syvak-php-fpm:
    build:
      context: $PWD
      dockerfile: $PWD/docker/dev/php/Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
        UMASK: ${UMASK:-0002}
    image: dev-syvak-php-fpm:1.0
    container_name: dev-syvak-php-fpm
    env_file:
      - $PWD/docker/dev/env/.env
      - $PWD/docker/dev/env/.env.mariadb
    volumes:
      - $PWD/httpdocs/app:/var/www/app
      - $PWD/httpdocs/httpdocs:/var/www/html
      - $PWD/httpdocs/storage:/var/www/storage
      - $PWD/docker/dev/php/php.ini:/usr/local/etc/php/php.ini:ro
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - dev-syvak-mariadb
    networks:
      - dev-syvak-net
      - buggregator-net
      - traefik-network

  dev-syvak-cron:
    build:
      context: $PWD
      dockerfile: $PWD/docker/dev/cron/Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
        UMASK: ${UMASK:-0002}
    image: dev-syvak-cron:1.0
    container_name: dev-syvak-cron
    restart: unless-stopped
    env_file:
      - $PWD/docker/dev/env/.env
      - $PWD/docker/dev/env/.env.mariadb
    volumes:
      - $PWD/httpdocs/app:/var/www/app:ro
      - $PWD/httpdocs/httpdocs:/var/www/html:ro
      - $PWD/httpdocs/storage:/var/www/storage:ro
      - $PWD/docker/dev/cron/crontabs:/etc/crontabs
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - dev-syvak-php-fpm
      - dev-syvak-mariadb
    networks:
      - dev-syvak-net
      - buggregator-net

  dev-syvak-nginx:
    build:
      context: $PWD
      dockerfile: $PWD/docker/dev/nginx/Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
        UMASK: ${UMASK:-0002}
    image: dev-syvak-nginx:1.0
    container_name: dev-syvak-nginx
    env_file:
      - $PWD/docker/dev/env/.env
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dev-syvak-nginx.rule=Host(`dev.syvak.com`)"
      - "traefik.http.routers.dev-syvak-nginx.entrypoints=web"
      - "traefik.http.services.dev-syvak-nginx.loadbalancer.server.port=80"
    volumes:
      - $PWD/httpdocs/app:/var/www/app:ro
      - $PWD/httpdocs/httpdocs:/var/www/html:ro
      - $PWD/httpdocs/storage:/var/www/storage:ro
      - $PWD/docker/dev/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    depends_on:
      - dev-syvak-php-fpm
    networks:
      - traefik-network
      - dev-syvak-net

  dev-syvak-mariadb:
    build:
      context: $PWD
      dockerfile: $PWD/docker/dev/mariadb/Dockerfile
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
        UMASK: ${UMASK:-0002}
    image: dev-syvak-mariadb:1.0
#    image: mariadb:10.11.5
    container_name: dev-syvak-mariadb
    ports:
      - "3340:3306"
    restart: no
    env_file:
      - $PWD/docker/dev/env/.env
      - $PWD/docker/dev/env/.env.mariadb
    volumes:
      - $PWD/db:/var/lib/mysql
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - dev-syvak-net

networks:
  traefik-network:
    name: traefik-network
    driver: bridge
    external: true
  dev-syvak-net:
    name: dev-syvak-net
    driver: bridge
    external: true
  buggregator-net:
    name: buggregator-net
    driver: bridge
    external: true